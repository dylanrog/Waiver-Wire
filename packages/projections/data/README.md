# projections/data

`rank_curves.json` is generated here by `scripts/build-rank-curves.ts` (Wave 1).
It maps positional rank to a mean and standard deviation of weekly fantasy points,
built offline from three seasons of nflverse data. It is committed and must never
be produced at request time. See `docs/ARCHITECTURE.md`.
